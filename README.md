# Symphonia Core-0 PC Tools

This repo contains the Python package `sym_cycles`, which hosts all
PC-side tools for communicating with Core-0 over UART and analyzing
its output (tiles, cycles, movement, etc.).

- Package: `sym_cycles`
- Location: `sym_cycles/`
